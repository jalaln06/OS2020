check_files_exist(){
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/search
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi
FILE=/libs/calc
if test -f "$FILE"; then
    echo "$FILE exists."
fi

}